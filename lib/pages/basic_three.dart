import 'package:flutter/material.dart';

class BasicOne extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Basic three')),
      
    );
  }
}